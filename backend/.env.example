# === バックエンド環境変数テンプレート ===
# このファイルをコピーして backend/.env を作り、値を入れてください。
# ※ .env 本体は絶対にコミットしないこと（キーが漏れる）

# ローカルのSQLite（そのままでOK）
DATABASE_URL=sqlite:///./takusu_local.db

# OpenAI APIキー（各自で用意。未設定でも動くが、理由文は簡易版になる）
OPENAI_API_KEY=